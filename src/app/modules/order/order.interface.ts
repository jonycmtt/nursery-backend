export type TOrder = {
  productId: string;
  name: string;
  email: string;
  price: number;
  quantity: number;
  phoneNumber: string;
  company: string;
  address: string;
  city: string;
  postCode: string;
  county: string;
};
